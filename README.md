# 🎤 Voice Dictation — Голосова диктовка для Linux

Системна утиліта для голосового введення тексту на Fedora Linux. Використовує Whisper для локального розпізнавання мови (працює офлайн!).

## Можливості

- 🎤 Запис голосу з мікрофона
- 🧠 Локальне розпізнавання через Whisper (без інтернету!)
- 🇺🇦 Підтримка української мови
- 🖥️ Іконка в системному треї
- 📋 Копіювання результату в буфер обміну
- ⚙️ Автоматичне копіювання в буфер обміну (опціонально)
- ⌨️ Глобальні гарячі клавіші для запуску/зупинки запису з будь-якої програми
- 📚 Історія диктовок з пошуком
- 🤖 Керування моделями Whisper (завантаження, видалення, встановлення за замовчуванням)
- ⚙️ Діалог налаштувань (GUI для редагування конфігурації)
- 📝 Файл конфігурації для налаштування поведінки програми

## Встановлення на Fedora

### 1. Системні залежності

```bash
# Базові інструменти розробки
sudo dnf install -y gcc gcc-c++ cmake pkg-config

# GTK4 для GUI
sudo dnf install -y gtk4-devel

# ALSA для аудіо
sudo dnf install -y alsa-lib-devel

# DBus для системного трея
sudo dnf install -y dbus-devel

# Clang/LLVM (потрібен для whisper-rs)
sudo dnf install -y clang llvm-devel
```

### 2. Rust (якщо ще не встановлено)

```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source ~/.cargo/env
```

### 3. Завантаження моделі Whisper

```bash
# Створюємо директорію для моделей
mkdir -p ~/.local/share/whisper

# Базова модель (~150MB) — швидка, прийнятна якість
curl -L -o ~/.local/share/whisper/ggml-base.bin \
    https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-base.bin
```

**Альтернативні моделі** (краща якість = більший розмір):

| Модель | Розмір | Якість | Швидкість |
|--------|--------|--------|-----------|
| `ggml-tiny.bin` | ~75MB | ⭐⭐ | Дуже швидка |
| `ggml-base.bin` | ~150MB | ⭐⭐⭐ | Швидка |
| `ggml-small.bin` | ~500MB | ⭐⭐⭐⭐ | Середня |
| `ggml-medium.bin` | ~1.5GB | ⭐⭐⭐⭐⭐ | Повільна |
| `ggml-large-v3.bin` | ~3GB | ⭐⭐⭐⭐⭐ | Дуже повільна |

Для української рекомендую `small` або `medium`:

```bash
# Small модель — хороший баланс
curl -L -o ~/.local/share/whisper/ggml-small.bin \
    https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-small.bin
```

### 4. Збірка програми

```bash
cd s2t
cargo build --release
```

Готовий бінарник буде в `target/release/voice-dictation`.

### 5. Встановлення (опціонально)

Використовуйте скрипт `install.sh` для автоматичного встановлення:

```bash
./install.sh
```

Або встановіть вручну:

```bash
mkdir -p ~/.local/bin
cp target/release/voice-dictation ~/.local/bin/
```

Переконайтеся, що `~/.local/bin` є в вашому `PATH`.

### 6. Запуск

```bash
# Якщо встановлено
voice-dictation

# Або безпосередньо з директорії проєкту
./target/release/voice-dictation
```

## Використання

### Основна робота

1. Після запуску з'явиться іконка мікрофона в системному треї
2. Клікніть на іконку → відкриється вікно диктовки
3. Натисніть **"🎤 Почати запис"**
4. Говоріть українською (або іншою мовою)
5. Натисніть **"⏹ Зупинити запис"**
6. Зачекайте на розпізнавання
7. Скопіюйте результат кнопкою **"📋 Копіювати"** (або автоматично, якщо увімкнено в конфігу)

### Історія диктовок

- Клікніть правою кнопкою миші на іконку в треї → **"Історія"**
- Або відкрийте меню в головному вікні → **"Історія"**
- Використовуйте поле пошуку для фільтрації записів
- Клікніть на запис, щоб скопіювати текст

### Керування моделями

- Клікніть правою кнопкою миші на іконку в треї → **"Моделі"**
- Або відкрийте меню в головному вікні → **"Моделі"**
- Завантажте нову модель: натисніть **"Завантажити"** біля потрібної моделі
- Встановіть модель за замовчуванням: натисніть **"Встановити за замовчуванням"**
- Видаліть модель: натисніть **"Видалити"** (модель за замовчуванням видалити неможливо)

### Налаштування

- Клікніть правою кнопкою миші на іконку в треї → **"Налаштування"**
- Або відкрийте меню в головному вікні → **"Налаштування"**
- Налаштуйте мову розпізнавання
- Увімкніть/вимкніть автоматичне копіювання
- Налаштуйте ліміти історії (максимальна кількість записів та вік)
- Увімкніть глобальні гарячі клавіші та налаштуйте комбінацію клавіш

### Глобальні гарячі клавіші

Після увімкнення в налаштуваннях, ви можете використовувати глобальну комбінацію клавіш (за замовчуванням `Control+Shift+Space`) для запуску або зупинки запису з будь-якої програми, без необхідності перемикатися на вікно диктовки.

## Системний трей на GNOME

GNOME за замовчуванням не показує іконки в треї. Встановіть розширення:

```bash
# Через dnf
sudo dnf install gnome-shell-extension-appindicator

# Або через GNOME Extensions
# https://extensions.gnome.org/extension/615/appindicator-support/
```

Після встановлення перезапустіть GNOME (Alt+F2 → `r` → Enter) або перелогіньтесь.

## Конфігурація

Програма зберігає налаштування у файлі `~/.config/voice-dictation/config.toml`. Файл створюється автоматично при першому запуску зі значеннями за замовчуванням.

### Доступні опції

```toml
# Модель Whisper за замовчуванням
default_model = "ggml-base.bin"

# Мова розпізнавання (uk, en, ru, тощо)
language = "uk"

# Максимальна кількість записів в історії
history_max_entries = 500

# Максимальний вік записів в історії (дні)
history_max_age_days = 90

# Автоматично копіювати результат в буфер обміну після розпізнавання
auto_copy = false

# Увімкнути глобальні гарячі клавіші
hotkey_enabled = false

# Комбінація клавіш для запуску/зупинки запису (формат: "Control+Shift+Space")
hotkey = "Control+Shift+Space"
```

### Розташування файлів

- **Конфігурація**: `~/.config/voice-dictation/config.toml`
- **Історія**: `~/.local/share/voice-dictation/history.json`
- **Моделі Whisper**: `~/.local/share/whisper/`

## Автозапуск

Якщо ви використовували `install.sh`, автозапуск вже налаштовано. В іншому випадку:

```bash
mkdir -p ~/.config/autostart

cat > ~/.config/autostart/voice-dictation.desktop << EOF
[Desktop Entry]
Type=Application
Name=Voice Dictation
Name[uk]=Голосова диктовка
Comment=Offline speech-to-text using Whisper
Comment[uk]=Офлайн розпізнавання мовлення через Whisper
Exec=$HOME/.local/bin/voice-dictation
Icon=audio-input-microphone
Terminal=false
Categories=AudioVideo;Audio;Utility;
Keywords=voice;dictation;speech;whisper;transcription;
StartupNotify=false
X-GNOME-Autostart-enabled=true
EOF
```

Переконайтеся, що бінарник встановлено в `~/.local/bin/voice-dictation`.

## Вирішення проблем

### Помилка "Не знайдено мікрофон"

```bash
# Перевірте, чи є мікрофон
arecord -l

# Перевірте PulseAudio/PipeWire
pactl list sources
```

### Помилка завантаження моделі

Переконайтеся, що модель завантажена правильно:

```bash
ls -la ~/.local/share/whisper/
# Має показати файл ggml-*.bin
```

### Погана якість розпізнавання

1. Спробуйте більшу модель (`small` або `medium`)
2. Говоріть чіткіше та ближче до мікрофона
3. Зменшіть фоновий шум

## Ліцензія

MIT

## Подяки

- [whisper.cpp](https://github.com/ggerganov/whisper.cpp) — швидка реалізація Whisper
- [whisper-rs](https://github.com/tazz4843/whisper-rs) — Rust bindings
- [GTK4-rs](https://gtk-rs.org/) — GTK bindings для Rust
